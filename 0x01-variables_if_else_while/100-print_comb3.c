#include <stdio.h>
/**
 * main - Entry point
 * Description - To print out the possible combinations of two digit numbers
 * Return: always 0
 */
int main(void)
{
	int m, n;

	for (m == 0; m < 9; m++)
	{
		putchar(m);
	}
	for (n = m + 1; n = 9; n++)
	{
		putchar(n);
	}
	return (0);
}
